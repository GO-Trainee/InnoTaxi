package pg

import "context"

func (s *pgRepository) FetchById(ctx context.Context, id int64) (*repositoryEnity.User, error) {
	// Implement the logic to fetch a record by its ID from the database.
	return nil, nil
}
