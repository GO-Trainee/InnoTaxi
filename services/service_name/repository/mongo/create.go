package mongo

import "context"

func (s *mongoRepository) Create(ctx context.Context, entity repositoryEnity.User) (*repositoryEnity.User, error) {
	// Implement the logic to create a new record in the database.
	return nil, nil
}
