package http

type HttpHandler struct {
	// http consumer
	// srv service.Service - reference to the service layer to call business logic
}

func New( /*http consumer, srv Service*/ ) *HttpHandler {
	return &HttpHandler{
		// initialize http consumer and service reference
	}
}
