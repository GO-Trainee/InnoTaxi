package http

// Non-proto HTTP endpoints go here.
// Proto-defined endpoints (e.g. POST /v1/invoices) are handled automatically
// by gRPC-Gateway — see http.go.
//
//
// func (h *HttpHandler) CreateRequested(w http.ResponseWriter, r *http.Request) {
//    // logic to handle the request, e.g. parse JSON body, call service layer, write response
// }
